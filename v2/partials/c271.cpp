#include <iostream>
#include <stdio.h>
#include <algorithm>

using namespace std;

char s[10000];

void st() {

	int len = strlen(s) - 1;
	while(len!=-1) {

		if(


	}
	cout << "YES" << endl;

}

int main() {
	while(!cin.eof() && (cin.getline(s,300))) {
	st();
	}
	return 0;
}
