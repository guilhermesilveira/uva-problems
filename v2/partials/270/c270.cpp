#include <iostream>
#include <stdio.h>

using namespace std;

char l[10000];

void st() {
	int i,j,k,l;
	while(1) {
		if(!gets(l) || sscanf(l,"%d %d",&k,&l)!=2) break;
		
	}
	
}

int main() {
int t;
	gets(l);
	sscanf(l,"%d",t);
	while(t--) st();
	return 0;


}
