#include <stdio.h>
#include <iostream>

using namespace std;

int main(char **argv,int argc) {

	register int casos, c = 0, i, a, b;

	while(true) {

		scanf(" %s", &a);
		if(a[0]=='0' && a[1]==0) return 0;
		
		

	}

	return 0;

}
